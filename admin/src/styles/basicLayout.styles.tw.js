import styled from 'styled-components';

// eslint-disable-next-line import/prefer-default-export
export const FullContainer = styled.div.attrs({
  className: 'flex flex-row bg-paris',
})`
  height: 100%;
  overflow: hidden;
  padding-top: 56px;
`;
